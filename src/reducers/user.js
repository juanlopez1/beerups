const initialState = {
    username: 'admin-one',
    password: '123456',
    details: {},
    login: false
};

const user = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;
