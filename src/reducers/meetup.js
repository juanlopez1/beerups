const initialState = {
    fetching: false,
    meetups: []
};

const meetup = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default meetup;
